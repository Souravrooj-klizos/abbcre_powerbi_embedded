/**
 * App auth helpers (e.g. session, user context for report mapping).
 * Optional: NextAuth, or custom session from your existing auth.
 */

export function getSessionUserId(): string | null {
  // TODO: read from NextAuth session or your auth provider
  return null;
}
